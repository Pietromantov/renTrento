export default {                                  
  setupFiles: ["dotenv/config"],
  collectCoverage: true,
  coverageDirectory: "coverage",   
  coverageProvider: "v8",
  testEnvironment: "node",
  verbose: true,
};