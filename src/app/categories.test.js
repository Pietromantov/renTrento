const request = require('supertest');
const app = require('./app');

/*
test('get', () => {
    expect.assertions(1);

    fetch("/categories")
    .then( res=> {
        expect( res.status ).toEqual(200)
    })

});*/