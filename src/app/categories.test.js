import request from 'supertest';
import app from './app.js';

/*
test('get', () => {
    expect.assertions(1);

    fetch("/categories")
    .then( res=> {
        expect( res.status ).toEqual(200)
    })

});*/